# Gateway API HTTPRoute for Portfolio
# Replaces ingress.yml

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: portfolio
spec:
  parentRefs:
    - name: main-gateway
      namespace: envoy-gateway-system
  hostnames:
    - nathanrahm.com
  rules:
    # Block /actuator externally (no backendRefs = 500)
    # Internal Prometheus scraping goes direct to pod IP, bypassing gateway
    - matches:
        - path:
            type: PathPrefix
            value: /actuator

    # Default route
    - backendRefs:
        - name: rahmnathan-portfolio
          port: 8080
